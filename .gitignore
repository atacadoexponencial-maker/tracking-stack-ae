# Secrets and local config — recipients must never commit these
wrangler.toml
wrangler.toml.audit
.dev.vars
.env
.env.*

# Cloudflare build artifacts
.wrangler/
.cloudflare/

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Node (if a recipient adds any tooling)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Astro build artifacts
dist/
.astro/

# Playwright MCP artifacts
.playwright-mcp/
*.jpeg

# o wrangler.toml do painel é config de projeto (sem secrets) e precisa versionar
!painel/wrangler.toml

# Documento de trabalho da auditoria tracking×ClickUp (não versionar — dados de leads)
docs/auditoria-tracking-clickup-julho-2026.md

# Aplicação da Google Ads API (não versionar — o repo é público e o doc traz o
# link secreto do painel de um cliente, que não tem senha)
docs/google-ads-api-application/
